# GlassGPT 5.4.0 Status

> Context-preservation note for future continuation: keep using the superpowers workflow already in progress here.
>
> Required process on resume:
> 1. `using-superpowers`
> 2. `systematic-debugging` for any new failure
> 3. `writing-plans` / review the existing 5.4.0 plan
> 4. `executing-plans`
> 5. `verification-before-completion`
> 6. `finishing-a-development-branch` only after CI/deploy/release are complete

## Current Branch

- Branch: `codex/stable-5.4`
- Starting point: created from `codex/stable-5.3`
- Current work is on this branch only

## Design And Plan Artifacts

- Design/spec: `docs/superpowers/specs/2026-03-30-glassgpt-5.4.0-design.md`
- Implementation plan: `docs/superpowers/plans/2026-03-30-glassgpt-5.4.0-release.md`

## Completed Feature Work

### 1. Image Upload

Implemented:
- route-aware body limits in `services/backend/src/http/app.ts` so image-bearing `/messages` requests are no longer capped by the 1 MB global default
- attachment-aware request validation in `packages/backend-contracts/src/conversation.ts`
- attachment-aware OpenAI input construction in `services/backend/src/adapters/openai/openai-responses.ts`
- attachment-only send acceptance in chat controller send flow

Evidence already collected:
- `corepack pnpm vitest run services/backend/src/http/app.test.ts services/backend/src/application/chat-run-service.test.ts services/backend/src/adapters/openai/openai-responses.test.ts --reporter=dot`
- result: passed
- focused iOS simulator suite covering request/controller attachment flow also passed

### 2. File Upload

Implemented:
- file upload path now benefits from the corrected route-aware body limits
- multipart upload request coverage was added on iOS
- chat attachment flow tests cover successful file upload ID forwarding
- failed upload lifecycle is verified in iOS tests
- chat upload loop now writes full `FileAttachment` values back into the observed array, which is safer for UI state propagation

Evidence already collected:
- backend upload proxy behavior covered in `services/backend/src/http/app.test.ts`
- focused iOS simulator suite covering:
  - `BackendClientRequestCoverageTests`
  - `NativeChatAttachmentFlowTests`
  - `NativeChatFileUploadLifecycleTests`
- result: passed

### 3. Tool Call Indicators

Implemented:
- normalized persisted tool-call JSON on write in `services/backend/src/application/live-payload-codec.ts`
- tolerant DTO mapping for legacy/null tool-call payloads in `services/backend/src/application/dto-mappers.ts`
- pre-completion persistence regression test in `services/backend/src/application/chat-run-service.test.ts`
- client grace-period configuration and helper plumbing in:
  - `BackendConversationProjectionController.swift`
  - `BackendChatController.swift`
  - `BackendAgentController.swift`
  - `BackendConversationProjectionController+StreamLifecycle.swift`
  - `BackendConversationRunStreamDriver.swift`
- focused iOS grace-period suite added

Evidence already collected:
- `corepack pnpm vitest run services/backend/src/application/live-payload-codec.test.ts services/backend/src/application/dto-mappers.test.ts services/backend/src/application/chat-run-service.test.ts --reporter=dot`
- result: passed
- focused iOS simulator suite:
  - `NativeChatToolCallGraceTests`
- result: passed

### 4. Sandbox File Download And Preview

Implemented:
- backend file download route improvements in `services/backend/src/http/routes/files.ts`
- fixed `container_id` query construction in `modules/native-chat/Sources/BackendClient/BackendClient+FileRequests.swift`
- exposed cache read/write operations via `GeneratedFileCacheManager`
- added `GeneratedFileInteractionCoordinator`
- wired generated-file preview/share state through:
  - `NativeChatShellState`
  - `NativeChatCompositionRoot`
  - `ContentView`
  - `BackendConversationSharedSections`
  - `BackendChatView` / `BackendAgentView`
  - message list sections
  - `MarkdownRichTextView`

Evidence already collected:
- backend route test:
  - `corepack pnpm vitest run services/backend/src/http/app.test.ts --reporter=dot`
  - result: passed
- focused iOS simulator suite:
  - `BackendClientRequestCoverageTests`
  - `NativeChatGeneratedFileInteractionTests`
  - result: passed

## Version Cutover Work

Implemented:
- `ios/GlassGPT/Config/Versions.xcconfig` bumped to `5.4.0` / `20223`
- backend/package metadata updated to `5.4.0`
- backend/version defaults updated in:
  - `services/backend/src/application/connection-check.ts`
  - `modules/native-chat/Sources/BackendClient/BackendEnvironment.swift`
  - `modules/native-chat/Sources/BackendContracts/BackendHealthDTOs.swift`
  - `modules/native-chat/Sources/NativeChatBackendCore/NativeChatCompositionRoot+Services.swift`
  - `scripts/deploy_backend.sh`
- generated OpenAPI metadata updated to `5.4.0`
- focused version-related backend/iOS tests updated and passing

Evidence already collected:
- backend targeted version tests:
  - `corepack pnpm vitest run services/backend/src/application/connection-check.test.ts services/backend/src/http/app.test.ts --reporter=dot`
  - result: passed
- focused iOS simulator version suites:
  - `BackendClientTests`
  - `BackendClientStreamingTests`
  - `SettingsAccountStoreDiagnosticsTests`
  - `SettingsAccountStoreCompatibilityTests`
  - result: passed

## Commits Created So Far

- `35f3e89` `docs: add 5.4.0 release design and plan`
- `526fb5c` `fix: restore attachment transport`
- `049969e` `fix: verify file upload lifecycle`
- `00f0a5b` `fix: restore tool call polling indicators`
- `3842e17` `fix: restore generated file downloads`
- `b827382` `chore: bump release version to 5.4.0`

## Full CI Status

### Backend CI

- Command: `./scripts/ci_backend.sh`
- Status: passing
- Fresh successful run already completed after:
  - adding OpenAI adapter coverage tests
  - fixing formatter issues
  - clearing the global coverage thresholds

### iOS CI

- Command: `./scripts/ci_ios.sh`
- Status: still in progress in the current session at the time this status note was written
- Latest known successful gates:
  - `ci-health`
  - `lint`
  - `python-lint`
  - `format-check`
  - `build`
  - `app-tests`
  - `package-tests` (after fixing stale controller expectations)
  - `architecture-tests`
- Latest known active gate:
  - `ui-tests`

## Important Fixes Made To Unblock Full iOS CI

- Restored agent-mode attachment blocking so agent conversations do not silently upload unsupported attachments
- Updated stale chat/agent controller coverage tests to match the polling-only runtime path
- Fixed SwiftFormat violations that the full iOS gate surfaced
- Added default no-op `onSandboxLinkTap` parameters where needed so existing snapshot/smoke tests keep compiling

## What Must Be Done Next

1. Let the current `./scripts/ci_ios.sh` run finish.
2. If `ci_ios.sh` fails:
   - read the first failing gate/test exactly
   - use `systematic-debugging`
   - make the minimum root-cause fix
   - rerun `./scripts/ci_ios.sh` from the top until clean
3. Once both full CI scripts are clean:
   - run `./scripts/deploy_backend.sh --env production`
   - verify production health/connection check shows backend version `5.4.0`
4. Then run:
   - `./scripts/release_testflight.sh 5.4.0 20223 --branch codex/stable-5.4 --skip-main-promotion`
5. After successful deploy + TestFlight:
   - `git push -u origin codex/stable-5.4`
   - `git tag v5.4.0`
   - `git push origin v5.4.0`
6. Only after all of the above succeed:
   - use `verification-before-completion`
   - then `finishing-a-development-branch`

## Resume Guidance

- Do not re-investigate the four feature implementations from scratch; the focused evidence above already exists.
- The highest-value next action is finishing the already-running full iOS CI gate.
- If context compacts, reopen this file first, then the design/spec, then the implementation plan.
