public enum GeneratedFilesBoundary {
    public struct Scope<Coordinator> {
        public let coordinator: Coordinator

        public init(coordinator: Coordinator) {
            self.coordinator = coordinator
        }
    }
}
